# --- Base de données & Config Sensible ---
# Si tu décides de mettre tes accès dans un fichier à part (recommandé)
config.php
.env

# --- Uploads (Médias des projets) ---
# On évite de push les images de test que tu as sur ton PC
# Mais on garde le dossier pour la structure
img/projects/*
!img/projects/.gitkeep

# --- OS & IDE ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Dépendances ---
vendor/
node_modules/